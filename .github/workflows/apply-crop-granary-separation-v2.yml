name: Crop granary branch smoke test

on:
  push:
    branches:
      - agent/crop-granary-separation-v2

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node --check game.js
